
import AnimalCard from "@/Components/AnimalCard";
import Banner from "@/Components/shared/Banner";

export default  function  Home() {


 // const bestAnimals = [...animals].sort((a,b) => a.price - b.price).slice(o,3);
  return (



   <div>
    <Banner></Banner>

    

   </div>
  );
}
